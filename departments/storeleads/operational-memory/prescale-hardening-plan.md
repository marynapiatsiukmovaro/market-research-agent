# Store Leads — Pre-Scale Hardening Plan (агенда S6+)

> **⚠️ СТАТУС: НИЧЕГО В ЭТОМ ДОКУМЕНТЕ НЕ УТВЕРЖДЕНО.** Это АГЕНДА — список вопросов и гипотез к
> обсуждению в S6. Любые «кандидат-решения» ниже = ПРЕДПОЛОЖЕНИЯ агента, НЕ правила. Что именно должно
> быть — выясняем в следующей сессии; решения принимает Marina. Агент НЕ внедряет ничего отсюда без явного OK.
>
> **Зачем этот документ.** S5 (2026-06-02) была диагностической: данные скрапера целы, но анализ обнулился,
> потому что агент читал/показывал лишь часть собранного. Вывод Marina: **к масштабу мы НЕ готовы** — правила
> есть, но нигде не ПРОВЕРЯЕТСЯ, что система им следует. На 250 магазинах это полсессии; на 20 000 — хаос.
> Поэтому ПЕРЕД анализом 2000 готовых магазинов сначала утверждаем структуру, шаг за шагом, с проверкой на
> каждом этапе.
>
> Читать ПЕРВЫМ в S6 (вместе с learnings HANDOFF S5). Статус каждого пункта: ⬜ обсудить → ✅ утверждено Marina.

---

## Что НЕ трогаем (это факты, проверены в S5 — не гипотезы)
- **Данные резервуара целы.** `np_batch9-16_enriched.json` (2000 магазинов) собраны `sl_enrich4` v4.2 — полный контракт, проверено по JSON b9. **Не пересобирать, не перезапускать скрапер.**
- **Decouple/резервуар-модель работает** — есть данные → старт сразу (Marina подтвердила ещё раз в S5).
- **processed_domains = 3848** (b9 помечен; b10/b11/b12 НЕ помечены и НЕ метить — анализ b9-12 недействителен, переделывается).

---

## Вопросы к обсуждению (по порядку) — НИ ОДИН не решён

### ⬜ 1. Финализировать stage2-карточку (эталонный контракт «до конца»)
- **Вопрос:** какой ПОЛНЫЙ набор полей и какая раскладка = наш эталон stage2?
- Для справки — текущий контракт в `methods/subagent-spec.md` перечисляет: `domain·pitch · tier · needs_live · score · country · created · visits · maturity · new_products_30d · cat_flag · store_type · product_class · conv · heroConf · descConf · pos · price-USD · in_range · 🏠banner+top-3 (фото·цена·in_range·descConf·pclass·desc·bullets) · social`. **Утвердить, что именно из этого нужно глазу Marina — а что лишнее.**
- Раскладка на выбор: **full** (21 колонка) vs **grouped** (11). Макеты: `~/Desktop/StoreLeads/Nursery & Playroom (S3 2026-06-01)/np_b9_MOCK_full.html` и `np_b9_MOCK_grouped.html`.
- *Кандидат-направление (гипотеза, НЕ решено):* один утверждённый эталон → один генератор.

### ⬜ 2. Единый генератор stage2 / судьба ad-hoc ридеров
- **Вопрос:** делаем ли `scripts/sl_stage2_table.py` единственным каноническим генератором, и запрещаем ли кустарные ридеры, показывающие лишь часть карточки?
- Контекст: именно расхождение канонического и кустарного `/tmp/sl_stage2_html.py` обнулило S5.
- *Кандидат-направление (гипотеза, требует OK Marina):* обновить канонический генератор до полного контракта; заретайрить `/tmp/sl_stage2_html.py`, `/tmp/sl_reader.py`, `/tmp/sl_stage2_v5.py`; возможно — op-rule «stage2 только каноническим генератором, подмножество карточки запрещено». **Как op-rule НЕ вводить без явного одобрения.**

### ⬜ 3. Правило проверки выхода скрапера против ЭТАЛОНА (главная дыра)
- **Вопрос:** как мы гарантируем на КАЖДОМ прогоне, что скрапер выдал полный контракт, а не «вчера тест прошёл»?
- Контекст: сейчас проверка разовая и ручная. Текущий `sl_qa.py` смотрит reach/price/cur, но НЕ полноту карточки.
- *Кандидат-направление (гипотеза):* расширить QA-gate на полноту контракта (% магазинов с 3 товарами, % товаров с картинкой, наличие banner/home_pitch, заполненность maturity/store_type/product_class/cat_flag/social, per-product in_range/descConf). Пороги PASS — обсудить и утвердить.

### ⬜ 4. Онбординг агента (входить правильно)
- **Вопрос:** что должен прочитать и каким инструментом пользоваться агент на старте, чтобы «начинать хорошо» каждый раз? (S5: агент начал не с того документа.)
- *Кандидат-направление (гипотеза):* чек-лист входа в `workflow.md` (preflight → генератор → QA-gate → эталон).

### ⬜ 5. Тест каждого этапа end-to-end (поиск узких мест)
- **Вопрос:** где ещё система тонкая? Прогнать b9 пошагово (stage1 → stage2 → QA → stage3), Marina видит вывод КАЖДОГО этапа, фиксируем узкие места до выхода на объём.

---

## Только ПОСЛЕ того, как Marina утвердит 1–5 — анализ
- Анализ 2000 магазинов резервуара (b9–b16) по утверждённой структуре, по одному батчу, checkpoint→STOP→OK Marina→Notion.
- Эталон формата отчёта stage3: `~/Desktop/StoreLeads/Nursery & Playroom (S3 2026-06-01)/np_batch2_stage3.html` (генератор stage3 ещё написать — пункт для S6).

## Незакрытые founder-keeps (в Notion после OK, не потерять)
- `maxandkoko.com` → Store Link 2 к существующей карточке Izimini (НЕ новая карточка).
- `miniware.eu/collections/hannie` → сохранить.
- В `shared/reported-products.md` уже помечены PENDING.

---
*Создан S5 2026-06-02 как АГЕНДА (не решения). Связано: learnings HANDOFF S5, `methods/subagent-spec.md`, `review/promotion-queue.md`, память `feedback_storeleads_full_card_3stage.md`.*
