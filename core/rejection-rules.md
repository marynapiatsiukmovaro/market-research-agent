# REJECTION RULES

Rejection logic is defined in `core/mandatory-filters.md`.
Apply mandatory filters before scoring. Never score a product that fails any filter.

→ Saturation level definitions: see `core/scoring-system.md`
→ Market-stage entry logic: see `core/winner-detection.md`
